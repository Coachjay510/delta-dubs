export default function FilmRoom() {
  return (
    <div style={{padding:'28px 24px'}}>
      <div style={{fontFamily:'var(--font-display)',fontSize:'clamp(36px,5vw,64px)',letterSpacing:'1px',lineHeight:.9,marginBottom:16,color:'var(--text)'}}>
        FILMROOM
      </div>
      <div style={{fontFamily:'var(--font-mono)',fontSize:11,color:'var(--np-green2)',letterSpacing:'2px',textTransform:'uppercase'}}>
        // Coming soon — Phase 2
      </div>
    </div>
  )
}
